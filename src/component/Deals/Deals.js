import React from 'react';
import Header from '../Header/Header';

const Deals = () => {
    return (
        <div>
            <Header></Header>
            <h3 className="text-center mt-5">There is no deals for you!</h3>
        </div>
    );
};

export default Deals;